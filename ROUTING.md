# Semantic routing demo

The pass-level Fireworks + vLLM-SR demo lives in [`fireworks-vllm-semantic-router/`](fireworks-vllm-semantic-router/). Intended standalone GitHub repo: `nagalikith/fireworks-vllm-semantic-router` (private until a signed result). This sandboxgrid product tree is unrelated.
