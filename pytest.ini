[pytest]
markers =
    tier1: Zero-tolerance tests that block merge/release
    tier2: Block release unless explicitly waived
    tier3: Warn but do not block POC
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
pythonpath = .
testpaths = tests
